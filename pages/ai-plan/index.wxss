.plan-page { padding-top: 34rpx; }
.plan-hero { margin-top: 12rpx; }
.plan-page .page-title { font-size: 52rpx; line-height: 1.35; }
.plan-lead { display: block; margin-top: 16rpx; color: var(--muted); font-size: 28rpx; line-height: 1.6; }
.state-card { margin-top: 30rpx; padding: 48rpx; border-radius: 30rpx; background: var(--paper); color: var(--muted); text-align: center; }
.rationale-card { margin-top: 30rpx; padding: 24rpx 26rpx; border-radius: 24rpx; background: var(--amber-soft); }
.rationale-card text { display: block; }
.rationale-card text:first-child { color: var(--amber-deep); font-size: 25rpx; }
.rationale-card text:last-child { margin-top: 10rpx; color: var(--brown); font-size: 28rpx; line-height: 1.65; }
.compare-card { margin-top: 18rpx; overflow: hidden; border: 1rpx solid rgba(137,111,82,.10); border-radius: 28rpx; background: var(--paper); }
.compare-row { display: flex; align-items: center; justify-content: space-between; min-height: 104rpx; padding: 0 26rpx; }
.compare-row + .compare-row { border-top: 1rpx solid rgba(94,75,57,.10); }
.compare-row > text { color: var(--muted); font-size: 28rpx; }
.compare-row view { display: flex; align-items: center; gap: 12rpx; font-size: 32rpx; font-weight: 650; }
.compare-row view text:nth-child(2) { color: var(--amber-deep); font-weight: 400; }
.compare-row view text:last-child { color: var(--amber-deep); }
.plan-boundary { margin-top: 20rpx; color: var(--muted); font-size: 24rpx; line-height: 1.65; }
.command-card { margin-top: 24rpx; padding: 28rpx; border-radius: 28rpx; background: var(--paper); }
.command-card text { display: block; }
.command-label { color: var(--amber-deep); font-size: 25rpx; }
.command-title { margin-top: 10rpx; font-size: 34rpx; font-weight: 650; }
.command-copy { margin-top: 9rpx; color: var(--muted); font-size: 27rpx; line-height: 1.55; }
.reconcile { margin-top: 20rpx; }
.apply-button { margin-top: 28rpx; }
.manual-button { margin-top: 14rpx; }
